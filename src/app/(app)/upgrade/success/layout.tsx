import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Upgrade Concluído',
};

export default function UpgradeSuccessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
