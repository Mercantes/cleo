# Story 4.2: Claude API Integration with Financial Context

## Status

**Status:** Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build", "test"]

## Story

**As a** user,
**I want** Cleo to answer my questions using my real financial data,
**so that** responses are personalized and useful, not generic.

## Acceptance Criteria

1. API route `POST /api/chat` receives user message and returns Cleo response via streaming (SSE)
2. System prompt includes: user name, monthly financial summary (income, expenses, balance), top 5 spending categories, active subscriptions, account balances
3. Context window built dynamically: fetch relevant data from Supabase before each call
4. Streaming response: response appears token by token in the interface
5. Supported questions: "Quanto gastei em alimentação?", "Posso fazer uma viagem de R$ 5.000?", "Como estão minhas assinaturas?", "Quanto terei daqui a 6 meses?"
6. Rate limiting: Free (30 msgs/month), Pro (unlimited) — enforcement via chat_usage table
7. Error handling: timeout, rate limit reached, API failure
8. Don't answer questions outside financial scope (redirect politely)

## CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Backend (API + AI Integration)
**Secondary Type(s)**: Streaming, Rate Limiting, Security
**Complexity**: High

### Specialized Agent Assignment

**Primary Agents**:
- @dev: Chat API route, context builder, streaming, rate limiting

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run before marking story complete
- [ ] Pre-PR (@devops): Run before creating pull request

### CodeRabbit Focus Areas

**Primary Focus**:
- Streaming implementation correctness
- Rate limiting enforcement
- Financial context accuracy

**Secondary Focus**:
- System prompt quality
- Error handling robustness

## Tasks / Subtasks

- [x] **Task 1: Create financial context builder** (AC: 2, 3)
  - [x] Create `src/lib/ai/financial-context.ts`

- [x] **Task 2: Create chat API route with streaming** (AC: 1, 4, 8)
  - [x] Create `src/app/api/chat/route.ts`

- [x] **Task 3: Implement rate limiting** (AC: 6)
  - [x] Create `src/lib/ai/chat-rate-limit.ts`

- [x] **Task 4: Integrate streaming in chat interface** (AC: 4)
  - [x] Update `src/components/chat/chat-interface.tsx`

- [x] **Task 5: Create Cleo system prompt** (AC: 2, 8)
  - [x] Create `src/lib/ai/system-prompt.ts`

- [x] **Task 6: Write tests** (AC: all)
  - [x] Create `tests/unit/ai/financial-context.test.ts`
  - [x] Create `tests/unit/ai/chat-rate-limit.test.ts`
  - [x] Create `tests/unit/chat/chat-route.test.ts`

## Dev Notes

### Streaming with ReadableStream

```typescript
// In route.ts:
import Anthropic from '@anthropic-ai/sdk';

const stream = await anthropic.messages.stream({
  model: 'claude-sonnet-4-20250514',
  max_tokens: 1024,
  system: systemPrompt,
  messages: conversationHistory,
});

return new Response(
  new ReadableStream({
    async start(controller) {
      for await (const event of stream) {
        if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
          controller.enqueue(new TextEncoder().encode(`data: ${JSON.stringify({ token: event.delta.text })}\n\n`));
        }
      }
      controller.enqueue(new TextEncoder().encode('data: [DONE]\n\n'));
      controller.close();
    },
  }),
  { headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache', Connection: 'keep-alive' } }
);
```

### Cleo System Prompt Template

```
Você é a Cleo, uma assistente financeira pessoal brasileira amigável e inteligente.

REGRAS:
- Responda APENAS perguntas sobre finanças pessoais
- Use dados reais do usuário (fornecidos abaixo)
- Valores em R$ (Real brasileiro)
- Responda em português do Brasil
- Seja concisa e acionável
- Se a pergunta não for sobre finanças, diga educadamente que você só ajuda com finanças

DADOS FINANCEIROS DO USUÁRIO:
{financialContext}
```

### Rate Limiting Table

```sql
-- Already in schema:
CREATE TABLE chat_usage (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  message_count INT NOT NULL DEFAULT 0,
  period TEXT NOT NULL, -- format: '2026-03'
  UNIQUE(user_id, period)
);
```

### Dependencies

- Story 4.1 (Chat Interface) — UI components
- Story 3.1 (AI Categorization) — categorized data for context
- ANTHROPIC_API_KEY configured ✅
- Anthropic SDK: `npm install @anthropic-ai/sdk` (may already be installed)

### Testing

- **Framework:** Vitest
- **Location:** `tests/unit/ai/`, `tests/unit/chat/`
- **Note:** Mock Anthropic SDK for streaming tests

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- Anthropic SDK mock requires class instead of vi.fn() for constructor
- financial-context.ts categories cast needs `unknown` intermediate for Supabase join type

### Completion Notes List
- Streaming SSE via ReadableStream, tokens sent as `data: {"token": "..."}`
- Financial context: profile name, monthly summary, top 5 categories, recurring, accounts
- Context cached 5 minutes to reduce DB queries
- Rate limiting: Free 30/month, Pro unlimited, tracked in chat_usage table
- System prompt includes scope guard (only financial questions)
- 135 tests passing, lint/typecheck/build clean

### File List
| File | Action | Description |
|------|--------|-------------|
| `src/lib/ai/system-prompt.ts` | Created | Cleo persona + scope guard |
| `src/lib/ai/financial-context.ts` | Created | Dynamic context builder with caching |
| `src/lib/ai/chat-rate-limit.ts` | Created | Rate limiting (free/pro tiers) |
| `src/app/api/chat/route.ts` | Created | POST with SSE streaming |
| `src/components/chat/chat-interface.tsx` | Modified | SSE stream reader + rate limit handling |
| `tests/unit/ai/financial-context.test.ts` | Created | 2 tests |
| `tests/unit/ai/chat-rate-limit.test.ts` | Created | 3 tests |
| `tests/unit/chat/chat-route.test.ts` | Created | 3 tests |

## QA Results
_To be filled by @qa_
