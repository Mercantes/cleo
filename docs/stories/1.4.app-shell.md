# Story 1.4: App Shell & Layout

## Status

**Status:** Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build", "test"]

## Story

**As a** authenticated user,
**I want** a responsive app layout with sidebar and navigation,
**so that** I can navigate between sections of the application.

## Acceptance Criteria

1. Authenticated layout with sidebar (desktop) + bottom navigation (mobile)
2. Navigation items: Dashboard, Chat, Transações, Projeções, Configurações
3. Header with user name, avatar and logout button
4. Responsive: sidebar collapses on mobile, bottom nav appears
5. Dashboard page with placeholder "Conecte seu banco para começar"
6. Skeleton loading states for page transitions
7. Reusable empty state component

## CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend / UI
**Secondary Type(s)**: Layout, Navigation
**Complexity**: Medium

### Specialized Agent Assignment

**Primary Agents**:
- @dev: Layout components, navigation, responsive design

**Supporting Agents**:
- @architect: Validate component architecture

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run before marking story complete
- [ ] Pre-PR (@devops): Run before creating pull request

### CodeRabbit Focus Areas

**Primary Focus**:
- Responsive layout correctness (mobile vs desktop)
- Server component vs client component boundaries

**Secondary Focus**:
- Accessibility (semantic HTML, ARIA labels)
- Performance (no unnecessary client components)

## Tasks / Subtasks

- [x] **Task 1: Install shadcn/ui components needed** (AC: 1, 2, 3)
  - [x] Install: `npx shadcn@latest add avatar dropdown-menu separator sheet skeleton tooltip`
  - [x] These components are used in sidebar, header, and loading states

- [x] **Task 2: Create AppSidebar component** (AC: 1, 2, 4)
  - [x] Create `src/components/layout/app-sidebar.tsx`:
    - Client component (`'use client'`)
    - Navigation items with icons (lucide-react):
      - Dashboard → `LayoutDashboard` icon → `/dashboard`
      - Chat → `MessageSquare` icon → `/chat`
      - Transações → `ArrowLeftRight` icon → `/transactions`
      - Projeções → `TrendingUp` icon → `/projections`
      - Configurações → `Settings` icon → `/settings`
    - Active state based on current pathname (`usePathname()`)
    - Cleo logo/branding at top
    - Hidden on mobile (`hidden md:flex`)
    - Width: `w-64` desktop, collapsible with toggle

- [x] **Task 3: Create BottomNav component** (AC: 1, 2, 4)
  - [x] Create `src/components/layout/bottom-nav.tsx`:
    - Client component (`'use client'`)
    - Same 5 navigation items as sidebar (icons only, labels below)
    - Active state based on current pathname
    - Fixed bottom position on mobile
    - Visible only on mobile (`md:hidden`)
    - Safe area padding for notch devices

- [x] **Task 4: Create Header component** (AC: 3, 7)
  - [x] Create `src/components/layout/header.tsx`:
    - Shows page title (dynamic based on route)
    - User section: avatar + name + dropdown menu
    - Dropdown: "Meu Perfil", "Configurações", separator, "Sair" (calls signOut action)
    - Mobile: hamburger menu to toggle sidebar (Sheet component)

- [x] **Task 5: Update (app) layout** (AC: 1, 3, 4)
  - [x] Update `src/app/(app)/layout.tsx`:
    - Fetch user profile from Supabase
    - Pass user data to Header and Sidebar
    - Layout structure with sidebar + header + main + bottom nav

- [x] **Task 6: Create EmptyState component** (AC: 7)
  - [x] Create `src/components/ui/empty-state.tsx`:
    - Reusable component with props: `icon`, `title`, `description`, `action` (optional CTA button)
    - Centered layout with subtle styling
    - Used across multiple pages for empty data states

- [x] **Task 7: Create Skeleton loading components** (AC: 6)
  - [x] Create `src/components/ui/page-skeleton.tsx`:
    - Reusable page-level skeleton using shadcn Skeleton
    - Variants: `dashboard`, `list`, `chat`
    - Used as loading.tsx files in route segments

- [x] **Task 8: Update Dashboard page** (AC: 5)
  - [x] Update `src/app/(app)/dashboard/page.tsx`:
    - Use EmptyState component: icon `Landmark`, title "Conecte seu banco para começar", description about connecting bank, CTA button "Conectar banco" (disabled, enabled in Story 2.2)
    - Show user greeting: "Olá, {name}!"
  - [x] Create `src/app/(app)/dashboard/loading.tsx`:
    - PageSkeleton variant="dashboard"

- [x] **Task 9: Create placeholder pages** (AC: 2)
  - [x] Create pages for remaining nav items with EmptyState:
    - `src/app/(app)/chat/page.tsx` — "Chat com IA em breve"
    - `src/app/(app)/transactions/page.tsx` — "Suas transações aparecerão aqui"
    - `src/app/(app)/projections/page.tsx` — "Projeções financeiras em breve"
    - `src/app/(app)/settings/page.tsx` — "Configurações"

- [x] **Task 10: Write tests** (AC: 1, 2, 7)
  - [x] Create `tests/unit/components/empty-state.test.tsx`:
    - Test renders title and description
    - Test renders action button when provided
    - Test renders icon
  - [x] Create `tests/unit/components/navigation.test.ts`:
    - Test navigation items array has 5 items
    - Test each item has label, href, and icon
  - [x] Verify `npm test` passes with all new tests

## Dev Notes

### Component Architecture (from system architecture doc)

```
src/components/
├── layout/
│   ├── app-sidebar.tsx       # Desktop sidebar navigation
│   ├── bottom-nav.tsx        # Mobile bottom navigation
│   └── header.tsx            # Top header with user menu
├── ui/
│   ├── empty-state.tsx       # Reusable empty state
│   └── page-skeleton.tsx     # Page-level loading skeleton
```

### Layout Pattern (from architecture doc)

```typescript
// src/app/(app)/layout.tsx
export default async function AppLayout({ children }) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();

  if (!user) redirect('/login');

  // Fetch profile for display
  const { data: profile } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', user.id)
    .single();

  return (
    <div className="flex h-screen">
      <AppSidebar user={profile} />
      <div className="flex-1 flex flex-col">
        <Header user={profile} />
        <main className="flex-1 overflow-auto p-4 md:p-6">{children}</main>
      </div>
      <BottomNav />
    </div>
  );
}
```

### Navigation Items

| Label | Icon | Route | Badge |
|-------|------|-------|-------|
| Dashboard | LayoutDashboard | /dashboard | — |
| Chat | MessageSquare | /chat | Pro (future) |
| Transações | ArrowLeftRight | /transactions | — |
| Projeções | TrendingUp | /projections | Pro (future) |
| Configurações | Settings | /settings | — |

### Responsive Breakpoints

- **Mobile** (< 768px): Bottom nav visible, sidebar hidden, hamburger menu in header
- **Desktop** (>= 768px): Sidebar visible, bottom nav hidden

### Dependencies on Story 1.3

- Auth middleware protecting `(app)/*` routes
- `(app)/layout.tsx` with auth check
- signOut server action for logout button

### Coding Standards

- **Server Components:** Layout is a Server Component (fetches profile)
- **Client Components:** Sidebar, BottomNav, Header need `'use client'` for interactivity (pathname, dropdowns)
- **Absolute Imports:** Always use `@/` prefix

### Testing

- **Framework:** Vitest + Testing Library
- **Location:** `tests/unit/components/`
- **Focus:** Component rendering tests and navigation data validation

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- shadcn v4 uses base-ui `render` prop instead of Radix `asChild` — fixed in header component
- Installed @testing-library/react, @testing-library/jest-dom, jsdom for component tests
- All 21 tests passing (4 empty-state + 3 navigation + 7 auth + 5 types + 2 example)
- lint, typecheck, build all passing

### Completion Notes List
- All 7 ACs implemented
- Sidebar (desktop) + bottom nav (mobile) with 5 nav items and active states
- Header with dynamic page title, user avatar/initials, dropdown menu with signOut
- Mobile hamburger menu using Sheet component
- EmptyState reusable component with optional CTA button
- PageSkeleton component with dashboard/list/chat variants
- Dashboard uses EmptyState with disabled "Conectar banco" CTA
- 4 placeholder pages (chat, transactions, projections, settings)

### File List
| File | Action | Description |
|------|--------|-------------|
| `src/components/layout/nav-items.ts` | Created | Shared navigation items data |
| `src/components/layout/app-sidebar.tsx` | Created | Desktop sidebar navigation |
| `src/components/layout/bottom-nav.tsx` | Created | Mobile bottom navigation |
| `src/components/layout/header.tsx` | Created | Header with user menu and mobile hamburger |
| `src/components/ui/empty-state.tsx` | Created | Reusable empty state component |
| `src/components/ui/page-skeleton.tsx` | Created | Page-level loading skeleton |
| `src/components/ui/avatar.tsx` | Created | shadcn Avatar component |
| `src/components/ui/dropdown-menu.tsx` | Created | shadcn DropdownMenu component |
| `src/components/ui/separator.tsx` | Created | shadcn Separator component |
| `src/components/ui/sheet.tsx` | Created | shadcn Sheet component |
| `src/components/ui/skeleton.tsx` | Created | shadcn Skeleton component |
| `src/components/ui/tooltip.tsx` | Created | shadcn Tooltip component |
| `src/app/(app)/layout.tsx` | Modified | Added sidebar, header, bottom nav layout |
| `src/app/(app)/dashboard/page.tsx` | Modified | Uses EmptyState component |
| `src/app/(app)/dashboard/loading.tsx` | Created | Dashboard loading skeleton |
| `src/app/(app)/chat/page.tsx` | Created | Chat placeholder page |
| `src/app/(app)/transactions/page.tsx` | Created | Transactions placeholder page |
| `src/app/(app)/projections/page.tsx` | Created | Projections placeholder page |
| `src/app/(app)/settings/page.tsx` | Created | Settings placeholder page |
| `src/app/layout.tsx` | Modified | Updated metadata title/description |
| `tests/unit/components/empty-state.test.tsx` | Created | 4 EmptyState component tests |
| `tests/unit/components/navigation.test.ts` | Created | 3 navigation items tests |
| `package.json` | Modified | Added testing-library deps |

## QA Results
_To be filled by @qa_
