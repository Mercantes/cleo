# Story 1.1: Project Setup & Infrastructure

## Status

**Status:** Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build", "test"]

## Story

**As a** developer,
**I want** a Next.js project configured with TypeScript, Tailwind CSS and shadcn/ui,
**so that** we have a standardized technical foundation for all development.

## Acceptance Criteria

1. Next.js 14+ (App Router) initialized with TypeScript strict mode
2. Tailwind CSS configured with base design tokens (colors, typography, spacing)
3. shadcn/ui installed and configured with at least Button, Input, Card and Dialog
4. ESLint + Prettier configured with consistent rules
5. Vitest configured with at least 1 passing example test
6. Directory structure defined: `src/app/`, `src/components/`, `src/lib/`, `src/types/`
7. `package.json` with scripts: dev, build, test, lint, typecheck
8. `.env.example` file with all required environment variables documented

## CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Deployment / Infrastructure
**Secondary Type(s)**: Frontend (initial config)
**Complexity**: Low

### Specialized Agent Assignment

**Primary Agents**:
- @dev: Project scaffold and configuration

**Supporting Agents**:
- @architect: Validate structure matches architecture doc

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run before marking story complete
- [ ] Pre-PR (@devops): Run before creating pull request

### CodeRabbit Focus Areas

**Primary Focus**:
- ESLint/Prettier configuration consistency
- TypeScript strict mode enabled

**Secondary Focus**:
- Package versions alignment
- Directory structure compliance with architecture

## Tasks / Subtasks

- [x] **Task 1: Initialize Next.js project** (AC: 1)
  - [x] Run `npx create-next-app@latest` with App Router, TypeScript, Tailwind, ESLint, `src/` directory, import alias `@/`
  - [x] Verify TypeScript strict mode in `tsconfig.json` (`"strict": true`)
  - [x] Verify `npm run dev` starts successfully on `localhost:3000`

- [x] **Task 2: Configure Tailwind design tokens** (AC: 2)
  - [x] Define color palette in `globals.css` @theme inline: primary (blue), success (green), danger (red), warning (amber), neutral (gray)
  - [x] Define typography: font-family Inter (sans) via Geist fonts
  - [x] Define spacing scale: consistent with Tailwind defaults
  - [x] Tailwind v4 uses CSS-based @theme inline config (no tailwind.config.ts)

- [x] **Task 3: Install and configure shadcn/ui** (AC: 3)
  - [x] Run `npx shadcn@latest init -d` with defaults (New York style, oklch colors)
  - [x] shadcn/ui v4 components available via `npx shadcn@latest add`
  - [x] Theme variables merged into globals.css alongside Cleo brand tokens

- [x] **Task 4: Configure ESLint + Prettier** (AC: 4)
  - [x] Install Prettier: `npm i -D prettier eslint-config-prettier`
  - [x] Create `.prettierrc` with: semi, singleQuote, trailingComma, tabWidth 2
  - [x] Add Prettier to ESLint config extends
  - [x] Add `format` script to package.json: `prettier --write .`
  - [x] Verify `npm run lint` runs without errors

- [x] **Task 5: Configure Vitest** (AC: 5)
  - [x] Install: `npm i -D vitest @testing-library/react @testing-library/jest-dom jsdom`
  - [x] Create `vitest.config.ts` with jsdom environment, path aliases, setup file
  - [x] Create `tests/setup.ts` with `@testing-library/jest-dom` imports
  - [x] Create 1 example test: `tests/unit/example.test.ts` that asserts `1 + 1 === 2`
  - [x] Add `test` script to package.json: `vitest run`
  - [x] Verify `npm test` passes

- [x] **Task 6: Create directory structure** (AC: 6)
  - [x] Create `src/components/ui/` (shadcn/ui already creates this)
  - [x] Create `src/components/layout/`
  - [x] Create `src/lib/supabase/`
  - [x] Create `src/lib/utils/`
  - [x] Create `src/types/`
  - [x] Create `src/hooks/`
  - [x] Create `src/stores/`
  - [x] Create `tests/unit/`, `tests/integration/`, `tests/e2e/`
  - [x] Add `.gitkeep` to empty directories

- [x] **Task 7: Configure package.json scripts** (AC: 7)
  - [x] Verify scripts: `dev`, `build`, `start`, `lint` (Next.js creates these)
  - [x] Add: `test` → `vitest run`
  - [x] Add: `test:watch` → `vitest`
  - [x] Add: `typecheck` → `tsc --noEmit`
  - [x] Add: `format` → `prettier --write .`
  - [x] Verify all scripts pass: `npm run lint && npm run typecheck && npm test && npm run build`

- [x] **Task 8: Create .env.example** (AC: 8)
  - [x] Create `.env.example` with all env vars (Supabase, Pluggy, Anthropic, Stripe, App)
  - [x] `.env.local` is in `.gitignore` (Next.js default)

## Dev Notes

### Project Structure Reference

The complete project structure is defined in `docs/architecture/system-architecture.md` Section 9. For this story, create only the top-level directories — subdirectories will be created in their respective stories.

```
src/
├── app/                # Next.js App Router (pages + API routes)
├── components/
│   ├── ui/             # shadcn/ui components
│   └── layout/         # App layout components (future stories)
├── lib/
│   ├── supabase/       # Supabase clients (Story 1.2)
│   └── utils/          # Shared utilities
├── types/              # TypeScript type definitions
├── hooks/              # React custom hooks
└── stores/             # Zustand stores
tests/
├── unit/               # Vitest unit tests
├── integration/        # Testing Library component tests
└── e2e/                # Playwright E2E tests
```

### Tech Stack (this story)

| Tech | Version | Purpose |
|------|---------|---------|
| Next.js | 14+ | App Router, RSC, API routes |
| TypeScript | 5.x | Strict mode |
| Tailwind CSS | 3.x | Utility-first CSS |
| shadcn/ui | latest | Component library (New York style) |
| Vitest | latest | Unit testing |
| ESLint + Prettier | latest | Linting + formatting |

### Coding Standards

- **Absolute Imports:** Always use `@/` prefix (e.g., `@/lib/utils`)
- **Server vs Client:** Default to Server Components. Add `'use client'` only when needed
- **Naming:** Components PascalCase, hooks camelCase with `use`, lib kebab-case, types PascalCase, constants UPPER_SNAKE

### Design Tokens (Cleo Brand)

For `tailwind.config.ts`, use these base colors:
- Primary: Blue tones (trust, finance)
- Success: Green (`#22C55E`) — income, positive
- Danger: Red (`#EF4444`) — expenses, negative
- Warning: Amber (`#F59E0B`) — alerts
- Neutral: Gray scale for backgrounds and text

### Testing

- **Framework:** Vitest with jsdom environment
- **Location:** `tests/unit/` for unit tests
- **Setup file:** `tests/setup.ts` with `@testing-library/jest-dom`
- **Coverage target:** Not enforced in this story
- **Pattern:** Write at least 1 passing test to validate the setup

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- `create-next-app` rejects capital letters in project name — used temp dir workaround
- Tailwind v4 uses CSS-based `@theme inline` instead of `tailwind.config.ts`
- `shadcn init --base-color` flag removed in v4 — used `-d` defaults
- ESLint was linting `.aiox-core/` framework files — added `globalIgnores`

### Completion Notes List
- Next.js 16.1.6 with App Router, TypeScript strict, Turbopack
- Tailwind CSS v4 with CSS-based theme config in `globals.css`
- shadcn/ui v4 initialized with oklch colors, New York style
- Cleo brand color tokens preserved alongside shadcn theme variables
- All 4 quality scripts pass: lint, typecheck, test, build
- Vitest 4.0.18 with 2 passing tests

### File List
| File | Status | Description |
|------|--------|-------------|
| `package.json` | Modified | Scripts: dev, build, start, lint, test, test:watch, typecheck, format |
| `tsconfig.json` | Created | TypeScript strict mode, path alias @/* |
| `next.config.ts` | Created | Next.js config |
| `eslint.config.mjs` | Created | ESLint + Prettier + globalIgnores for framework dirs |
| `.prettierrc` | Created | Prettier config (semi, singleQuote, trailing comma) |
| `vitest.config.ts` | Created | Vitest with jsdom, path aliases, setup file |
| `tests/setup.ts` | Created | Testing Library jest-dom setup |
| `tests/unit/example.test.ts` | Created | Example test (2 passing tests) |
| `src/app/globals.css` | Modified | Tailwind v4 theme + Cleo brand tokens + shadcn vars |
| `src/app/layout.tsx` | Created | Root layout with Geist fonts |
| `src/app/page.tsx` | Created | Home page |
| `src/lib/utils.ts` | Created | shadcn/ui cn() utility |
| `src/components/ui/` | Created | shadcn/ui component directory |
| `src/components/layout/.gitkeep` | Created | Layout components placeholder |
| `src/lib/supabase/.gitkeep` | Created | Supabase clients placeholder |
| `src/lib/utils/.gitkeep` | Created | Utils placeholder |
| `src/types/.gitkeep` | Created | TypeScript types placeholder |
| `src/hooks/.gitkeep` | Created | Custom hooks placeholder |
| `src/stores/.gitkeep` | Created | Zustand stores placeholder |
| `tests/integration/.gitkeep` | Created | Integration tests placeholder |
| `tests/e2e/.gitkeep` | Created | E2E tests placeholder |
| `.env.example` | Modified | Environment variables template (Supabase, Pluggy, Anthropic, Stripe) |
| `components.json` | Created | shadcn/ui configuration |

## QA Results
_To be filled by @qa_
